<%= form_for(@tadress) do |f| %>
  <% if @tadress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tadress.errors.count, "error") %> prohibited this tadress from being saved:</h2>

      <ul>
      <% @tadress.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country %><br>
    <%= f.text_field :country %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
