<%= form_for(@user_animal) do |f| %>
  <% if @user_animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_animal.errors.count, "error") %> prohibited this user_animal from being saved:</h2>

      <ul>
      <% @user_animal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_profiles_id %><br>
    <%= f.number_field :user_profiles_id %>
  </div>
  <div class="field">
    <%= f.label :animals_id %><br>
    <%= f.number_field :animals_id %>
  </div>
  <div class="field">
    <%= f.label :load %><br>
    <%= f.number_field :load %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
