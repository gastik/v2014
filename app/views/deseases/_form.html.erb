<%= form_for(@desease) do |f| %>
  <% if @desease.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@desease.errors.count, "error") %> prohibited this desease from being saved:</h2>

      <ul>
      <% @desease.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
